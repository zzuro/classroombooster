export interface Subject {
    school: string;
    subjects: Array<string>;
    img: string;
  }
  
  export interface Person {
    name: string;
    profession: string;
    desc: string;
    img: string;
  }